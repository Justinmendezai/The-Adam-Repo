# Handoffs

Archived session handoffs for **your** product work. Do not commit private project notes into a public fork of Adam.

In a product repo, write dated files here (or under `scratch/handoff/`) via the `handoff` / `context-primer` skills.
