# Claude Code — Adam

Read [`AGENTS.md`](AGENTS.md) first. Same harness, Claude-native paths:

| What | Path |
|------|------|
| Skills (this repo) | `.claude/skills/<name>/SKILL.md` |
| Skills (user install) | `~/.claude/skills/<name>/` |
| Rules | [`foundation/rules.md`](foundation/rules.md) |
| Config (target projects) | `adam.json` |

Install user skills with `./scripts/install-skills.sh claude` only if calibrate could not. Copy **folders**, never a pile of `SKILL.md` files.

Drive in natural language. Do not tell the operator to type slash commands.
