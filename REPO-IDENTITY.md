# Repo identity — read before first-run edits

This is the **public** Adam harness.

| | |
|--|--|
| Public | https://github.com/Justinmendezai/The-Adam-Repo |
| Confirm | `git remote -v` shows `Justinmendezai/The-Adam-Repo` |
| Clone path | often `~/adam` or `~/The-Adam-Repo` |

**Not this repo:** `https://github.com/slowcoder360/adam` (private factory).

If origin is `slowcoder360/adam`, stop. Do not change README install copy, `docs/bootstrap.md`, `calibrate`, or Codex/ChatGPT first-run here. Open The-Adam-Repo.

Agents: run `./scripts/assert-public-repo.sh` before publishing first-run copy.
